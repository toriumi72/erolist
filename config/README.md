# config/ README

このディレクトリは **設定値・環境変数のラッパー** を置きます。

## ここに置くもの

- `process.env` の読み取りを 1 箇所に集約するラッパー
- Zod 等での **環境変数バリデーション**
- 環境ごとの差分（開発/本番）を吸収する設定

## ここに置かないもの

- 秘密情報の直書き（禁止）
- ビジネスロジック（→ `services/`）

## 典型例

- `config/env.ts`（Zod で env チェック）
- `config/app-config.ts`


