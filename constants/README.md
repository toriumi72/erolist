# constants/ README

このディレクトリは **定数（変更されにくい値）** を置きます。

## ここに置くもの

- ランキング種別、ソート種別、ページサイズなどの定数
- 画面表示用ラベル、メッセージ（国際化するなら別方針でもOK）
- 正規表現、閾値、制限値

## ここに置かないもの

- 環境変数（→ `config/`）
- 設定が頻繁に変わるデータ（→ DB や `config/` へ）

## 典型例

- `constants/pagination.ts`
- `constants/ranking.ts`


